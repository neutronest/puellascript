{-# LANGUAGE CPP #-}

module Language.PuellaScript.Compiler.SelfBuildInfo where

ghc :: FilePath
ghc = GHC

ghcPkg :: FilePath
ghcPkg = GHC_PKG

libDir :: FilePath
libDir = LIBDIR

pkgDb :: FilePath
pkgDb = PKGDB

phc :: FilePath
phc = PHC
